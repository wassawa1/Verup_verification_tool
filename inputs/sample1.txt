これはサンプルテキストファイルです。
このファイルは入力ファイルとして使用されます。
各バージョンのツールによって異なる処理が行われます。

SampleTool 1.0.0 では、基本的な統計情報を生成します。
SampleTool 2.0.0 では、より高度な単語頻度解析が追加されました。

実行結果は、artifactsディレクトリに保存されます。
バージョン間の比較結果は、CSVおよびHTMLレポートとして出力されます。

このシステムは、ツールのアップデートによる動作の違いを検証するために使用されます。
入力ファイルを与えることで、実際のユースケースでの検証が可能になります。
